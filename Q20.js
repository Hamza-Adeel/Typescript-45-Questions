// Q20).Think of something you could store in a favoritePlaces.
// For example, you could make a list of mountains, rivers, countries, 
//cities, languages, or anything else you’d like. 
//Write a program that creates a list containing these items.
let favoritePlaces = ["America, Washington", "Japan,Tokyo", "Italy, Rome", "China,Shanghai", "Pakistan,Islamabad"];
for (let i = 0; i < favoritePlaces.length; i++) {
    const element = favoritePlaces[i];
    console.log(element);
}
export {};
